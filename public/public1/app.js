/* Shared tiny helper.
   If some links accidentally open Safari instead of staying in standalone, ensure you don't use target="_blank".
   Optional debug (commented):
   console.log('standalone:', window.navigator.standalone === true || window.matchMedia('(display-mode: standalone)').matches);
*/
